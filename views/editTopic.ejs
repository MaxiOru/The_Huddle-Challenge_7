<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Tema</title>
</head>
<body>
    <!-- Enlace al controlador: GET / (Página principal - lista de temas) -->
    <p><a href="/">&lt; Volver al inicio</a></p>

    <h1>Editar Tema</h1>

    <!-- Formulario conectado al controlador: POST /edit-topic/:id (Actualizar tema) -->
    <form action="/edit-topic/<%= topic.id %>" method="POST">
        <p>
            <label for="title">Título del Tema:</label><br>
            <input type="text" id="title" name="title" value="<%= topic.title %>" required style="width: 300px;">
        </p>
        <p>
            <button type="submit">Actualizar Tema</button>
            <!-- Enlace al controlador: GET / (Página principal - lista de temas) -->
            <a href="/">Cancelar</a>
        </p>
    </form>
</body>
</html>